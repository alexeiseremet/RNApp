/**
 * @format
 */

import {StyleSheet} from 'react-native';

const stylesFn = () =>
  StyleSheet.create({
    HomeScreen: {
      flex: 1,
    },
    HomeScreenContainer: {
      flex: 1,
      justifyContent: 'center',
    },
  });

export default stylesFn;
